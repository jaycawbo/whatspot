GOAL: Add remaining missing keyword mappings and a name/category text search fallback to recommend/index.ts.

PROBLEM: Two things still needed after the previous changes:
1. A second batch of keyword mappings not yet added to Block A and Block B
2. A name/category text search fallback so venues with inconsistent venue_types tags still surface (e.g. a steakhouse tagged only as "restaurant" is invisible to a steak type-match query)

PART 1 — Add these mappings to both Block A (Supabase filter) and Block B (LLM candidate sort):
- mediterranean → mediterranean_restaurant, greek_restaurant
- middle eastern → middle_eastern_restaurant
- turkish → turkish_restaurant
- lebanese → lebanese_restaurant, middle_eastern_restaurant
- persian → persian_restaurant, middle_eastern_restaurant
- ethiopian → ethiopian_restaurant, african_restaurant
- latin → latin_american_restaurant, mexican_restaurant
- peruvian → peruvian_restaurant, latin_american_restaurant
- portuguese → portuguese_restaurant
- dim sum, dumplings → chinese_restaurant, dim_sum_restaurant
- tacos → mexican_restaurant, taco_restaurant
- noodles, pho → vietnamese_restaurant, ramen_restaurant, noodle_restaurant

PART 2 — Add name/category text search fallback to the Supabase search path:
After the existing type-matched Supabase query runs and before any Google fallback fires, add a second Supabase lookup that triggers when type-matched results return fewer than 8 venues:
1. Run a text search against name and category columns using the refined search term
2. Use Supabase ilike: .or('name.ilike.%term%,category.ilike.%term%')
3. Apply same admission filter (rating >= 3.8, review_count >= 15) and same geographic radius
4. Deduplicate against type-matched results already found
5. Merge results — type-matched venues first, name/category matches appended after
6. Only fall through to Google if combined result of both lookups is still fewer than 5 venues

Do not touch: DiscoveryDeck.jsx, DiscoveryCard.jsx, ConstellationsSheet.jsx, useDiscoveryInteractions.js, Home.jsx, useDiscoveryFeed.js, Spots.jsx